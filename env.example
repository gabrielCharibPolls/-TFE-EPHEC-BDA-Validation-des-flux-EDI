# Copier vers .env (docker compose charge .env automatiquement)
DATABASE_URL=postgresql://edi:edi@localhost:5433/edi_validation
EDI_INPUT_DIR=./in
# VALIDATOR_MAX_FILES=50

# --- Alerting (optionnel) — déclenché sur KO et PARSE_ERROR ---
# TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/...
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_TLS=1
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=edi-validator@scabel.local
# SMTP_TO=support-it@scabel.local
